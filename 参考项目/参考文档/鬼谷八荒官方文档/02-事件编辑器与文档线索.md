# 事件编辑器与文档线索

## 1. 事件编辑器 PDF 原件

来源线索：
`https://file.conchship.com.cn/event/%E4%BA%8B%E4%BB%B6%E7%BC%96%E8%BE%91%E5%99%A8%E4%BD%BF%E7%94%A8%E8%AF%B4%E6%98%8E.pdf`

当前已落盘原件：

- [事件编辑器使用说明.pdf](/d:/Projects/AllGameInAI/参考项目/参考文档/鬼谷八荒官方文档/事件编辑器使用说明.pdf)
  - 大小：`16,490,804` bytes

已核对信息：

- 文档标题为 `事件编辑器使用说明`。
- 已从 PDF 正文中抽到配置表示例代码。
- 已从 PDF 正文中抽到这些关键字段：
  - `ConfigName`
  - `SrcConfigRefName`
  - `DestConfigRefName`
  - `TemplateId`
- PDF 正文明确提到：
  - 可以以一个已存在的配置数据为模板来修改
  - 例子中演示了以已有武器为模板新增一个物品配置项

这条线索非常关键，因为它直接指向：

- 官方文档里存在“以已有配置项为模板新增物品”的说明
- 这和我们后续“复用现有物品模板，再生成新的实例表现”高度相关

## 2. 当前抽取结果

当前已经能明确确认：

- 这份 PDF 不是空壳或无效链接，原件已成功下载。
- 这份 PDF 里确实存在配置表编辑说明。
- 这份 PDF 里确实存在“以已有配置项为模板新增物品”的写法。
- 这份 PDF 当前未直接抽到这些关键词：
  - `GGBH_API`
  - `modFQA`
  - `Excel`
  - `CHM`

这说明：

- `事件编辑器使用说明.pdf` 主要解决的是“事件编辑器怎么用、怎么改配置项”。
- `GGBH_API.chm` 和配置 Excel 更像是另一组教程包，不一定写在这份 PDF 里。

## 3. 旧问题已解决 / 新问题仍存在

之前的旧问题：

- 对 `file.conchship.com.cn` 直链抓取时一度出现 TLS/Timeout 问题。

当前状态：

- 已成功拿到 PDF 原件。
- 不再只是保存摘要。

仍然存在的新问题：

- 还没有拿到与之配套的 `配置对照表.xlsx` 原件。
- 还没有拿到 `GGBH_API.chm` 原件。

## 4. 工程意义

仅依据当前已核对的 PDF 正文，可以做出这条更硬的判断：

- 官方文档里至少存在“模板引用 + 新配置项”这类能力说明
- 这支持后续重点验证：
  - 现有物品模板复用
- 物品配置改写
  - 是否能通过配置/事件结合实现奖励发放

但目前还不能仅凭这份 PDF 断言：

- 一定能直接改“单个物品实例”的名称与描述
- 一定能无缝走玩家背包入包逻辑

这两点仍然需要结合：

- 官方示例
- `配置对照表.xlsx`
- `GGBH_API.chm`
- 具体开源 mod

继续验证。
