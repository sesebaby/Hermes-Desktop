# Superpowers Legacy Archive

本目录保存 `2026-04-07` 重写前的整套 `docs/superpowers` 文档。

用途：

1. 保留旧设计、旧合同、旧施工蓝图、旧证据说明，避免信息丢失。
2. 作为后续核对与迁移参考。
3. 不再作为当前 authority。

使用规则：

1. 本目录下所有内容统一视为 `legacy reference`。
2. 若与当前 `docs/superpowers/` 冲突，以当前 `docs/superpowers/` 为准。
3. 只有在核对历史设计、回查旧口径、迁移遗漏内容时才读取本目录。
