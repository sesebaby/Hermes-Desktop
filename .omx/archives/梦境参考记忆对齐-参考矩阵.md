# 参考实现矩阵：梦境参考记忆对齐

| 能力 | 参考锚点 | 参考原始行为 | 当前项目映射 | 当前状态 | 允许偏差 | 禁止漂移检查 | 证据 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 精选记忆的持久化真源 | `external/hermes-agent-main/agent/memory_provider.py`, `agent/memory_manager.py`, `tools/memory_tool.py` | 通过受限的 memory provider/tool 语义维护持久化的 `MEMORY.md` / `USER.md`。 | `src/memory/MemoryManager.cs`, `src/Tools/MemoryTool.cs`, `src/memory/MemoryReviewService.cs` | 已对齐 | 保持 C# `MemoryManager` 作为持久权威。 | Dreamer 和 AutoDream 不能成为写入权威。 | `MemoryToolTests`、`MemoryReviewServiceTests`、`MemoryParityTests`，完整测试运行 659 通过 / 1 跳过。 |
| 冻结的精选提示词快照 | `agent/prompt_builder.py`、memory 文档 | 精选记忆是提示词上下文，不等同于转录检索。 | `src/plugins/BuiltinMemoryPlugin.cs`、`src/memory/CuratedMemoryLifecycleProvider.cs` | 已对齐 | 保留每会话冻结的插件快照，并在压缩边界刷新。 | 精选记忆不得通过 `<memory-context>` 动态重新发出。 | `BuiltinMemoryPlugin_*`、`CuratedMemoryLifecycleProvider_Prefetch_IsInert`、`TurnMemoryCoordinator_DoesNotInjectCuratedMemoryThroughDynamicRecall`。 |
| 动态转录/会话回忆 | `tools/session_search_tool.py`、提示词指南 | `session_search` 独立于精选记忆回忆过去的转录。 | `src/search/TranscriptMemoryProvider.cs`、`src/search/TurnMemoryCoordinator.cs`、`src/Tools/SessionSearchTool.cs` | 已对齐 | 保持当前 C# 回忆 provider 形态。 | 不要把 `memory` 和 `session_search` 工具契约合并。 | 现有 `MemoryParityTests` 和 `SessionSearchTool` 测试。 |
| 记忆 provider 生命周期 | `agent/memory_provider.py`、`agent/memory_manager.py` | provider 接收生命周期钩子，失败彼此隔离。 | `src/memory/IMemoryProvider.cs`、`src/memory/IMemoryCompressionParticipant.cs`、`src/memory/HermesMemoryOrchestrator.cs` | 已对齐 | 增加独立的压缩参与者契约。 | 不要让生命周期参与等同于动态提示词注入。 | `HermesMemoryOrchestrator_PreCompressAll_CallsParticipantsInOrder`、`HermesMemoryOrchestrator_PreCompressAll_ParticipantFailuresAreNonFatal`。 |
| 压缩前交接 | `agent/memory_provider.py`、`agent/memory_manager.py`、`run_agent.py` | 记忆管理器在上下文压缩/轮换前调用 provider 的 pre-compression 钩子。 | `src/Context/ContextManager.cs` 以及 `HermesMemoryOrchestrator` | 已对齐 | 在活动的 `ContextManager` 压缩接缝处实现；本次不引入参考实现中的 session-id 轮换。 | 交接必须在插件 pre-compression 和摘要生成/替换之前运行。 | `ContextManager_PreCompressesMemoryBeforePluginsAndSummarizingEvictedMessages`。 |
| Dreamer 后台功能 | 无独立参考模块；参考等价物是学习/回忆循环 | 参考实现没有单独的 C# 风格 Dreamer 循环。 | `src/dreamer/*`、`Desktop/HermesDesktop/App.xaml.cs` | 通过标题本地化适配对齐 | 保持现有 Dreamer 行为和测试。 | 不要让 Dreamer 默认成为记忆权威。 | Dreamer/RssFetcher/InsightsDreamer 过滤：160 通过。 |
| 休眠的 AutoDream | 无直接强制对齐目标 | 参考学习循环不要求当前休眠的 `AutoDreamService`。 | `src/dream/AutoDreamService.cs` | 已对齐 | 之后仅在单独批准下再挖掘想法。 | 不要默认注册/启动。 | `DesktopStartup_DoesNotRegisterAutoDreamServiceByDefault`。 |

## 实施后验证

- `dotnet test .\Desktop\HermesDesktop.Tests\HermesDesktop.Tests.csproj --filter "FullyQualifiedName~MemoryParityTests|FullyQualifiedName~MemoryToolTests"`：80 通过，0 失败，0 跳过。
- `dotnet test .\Desktop\HermesDesktop.Tests\HermesDesktop.Tests.csproj --filter "FullyQualifiedName~Dreamer|FullyQualifiedName~RssFetcher|FullyQualifiedName~InsightsDreamer"`：160 通过，0 失败，0 跳过。
- `dotnet test .\Desktop\HermesDesktop.Tests\HermesDesktop.Tests.csproj -c Debug -p:Platform=AMD64`：659 通过，1 跳过。
- `dotnet build .\HermesDesktop.slnx --no-restore`：0 警告，0 错误。
- `dotnet build .\Desktop\HermesDesktop\HermesDesktop.csproj -c Debug -p:Platform=x64 --no-restore`：构建成功，存在 1 个已有的 Stardew mod 平台警告，0 错误。
