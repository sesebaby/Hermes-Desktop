# Python Memory Reference Deviation Approvals

| Date | Ability | Drift Layer | Reference Behavior | Current Deviation | Why Required | Category | Approval | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-04-27 | transcript authority | Authoritative Writeback | Python stores/searches session memory through SQLite `SessionDB` and FTS5 | C# keeps JSONL `TranscriptStore` authoritative and treats SQLite FTS as derived | The existing desktop app already persists transcripts as JSONL; migrating storage now would be a larger data migration than memory parity requires | title-local adaptation | approved | Captured in existing autopilot spec; no behavior should depend on SQLite being authoritative |
| 2026-04-27 | auxiliary session summarization | Prompt / Summary Assembly | Python uses configured auxiliary LLM task `session_search` with bounded concurrency | C# uses optional `IChatClient` summarization and falls back to deterministic previews | The C# app has one swappable chat client today and no separate auxiliary-model config; fallback preserves the Python no-drop behavior | title-local adaptation | approved | Future work can add dedicated auxiliary model config behind the same service method |
| 2026-04-27 | background memory review | Projector / Executor | Python spawns a full background `AIAgent` fork with inherited runtime and tools | C# runs a detached `IChatClient.CompleteWithToolsAsync` review exposing only the Python-compatible `memory` tool | The C# app has no safe full-agent fork lifecycle for post-turn review yet; this preserves non-blocking post-response memory review and avoids mutating the main session history | title-local adaptation | implemented under `$ralph` parity request | Do not call this a full agent-fork port; it is equivalent for memory writes only, not skill-review parity |
