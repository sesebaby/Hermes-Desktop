# Runtime-only OMX artifacts.
# Project documents under `.omx/` are intended to be versioned; the repo root
# `.gitignore` explicitly re-includes `.omx/**` to override local excludes.
logs/
state/
/*.json
