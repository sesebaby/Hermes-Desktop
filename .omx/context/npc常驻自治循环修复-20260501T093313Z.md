## 任务陈述

修复最后一次提交后仍然存在的关键缺口：Stardew NPC 的 autonomy 仍然不是“每个 NPC 常驻 Hermes agent 循环”，而只是共享了 runtime handle 的单次 debug tick。

## 期望结果

- Desktop 启动后，目标 NPC 的 autonomy 路径会像参考项目那样常驻运行，而不是只能手动点一次 tick。
- 每个 NPC 继续使用自己的 runtime namespace / SOUL / transcript / memory。
- private-chat 与 autonomy 共用同一个 NPC runtime 生命周期所有权，不再各自漂移。
- 方案严格遵守当前仓库约束：单 Desktop 进程内托管多个 NPC runtime，不改成“一 NPC 一进程”。

## 已知事实 / 证据

1. 最后一次提交 `396c2de2c1cf584f9675bc0938a568082870e7e4` 主要完成的是“共享装配 / 共享 handle / 共享 supervisor ownership”。
   - `src/runtime/NpcRuntimeSupervisor.cs`
   - `src/runtime/NpcRuntimeInstance.cs`
   - `src/runtime/NpcRuntimeBindings.cs`
   - `src/games/stardew/StardewAutonomyTickDebugService.cs`
   - `src/games/stardew/StardewPrivateChatOrchestrator.cs`

2. 现在真正被 Desktop 启动接线拉起的后台服务只有 private-chat。
   - `Desktop/HermesDesktop/App.xaml.cs:408-412`
   - `Desktop/HermesDesktop/App.xaml.cs:673-679`

3. `autonomy` 仍然只有单次调试入口。
   - `src/games/stardew/StardewAutonomyTickDebugService.cs:90-130`
   - `Desktop/HermesDesktop/Views/DashboardPage.xaml.cs:441-450`

4. `NpcRuntimeHost` 并没有被任何启动链真正调用来拉起 discovered NPC runtime。
   - `src/runtime/NpcRuntimeHost.cs:23-30`
   - 代码库全文检索仅发现注册点，没有发现 `StartDiscoveredAsync(...)` 调用点。

5. `NpcRuntimeInstance.StartAsync(...)` 只建目录并把状态置为 `Running`，没有后台任务、轮询循环、取消令牌、重启或定时调度。
   - `src/runtime/NpcRuntimeInstance.cs:29-39`

6. 现有新增测试主要覆盖“handle 复用”和“单次 tick 共用装配”，没有覆盖“常驻 autonomy loop 已自动运行”。
   - `Desktop/HermesDesktop.Tests/Runtime/NpcRuntimeSupervisorTests.cs`
   - `Desktop/HermesDesktop.Tests/Stardew/StardewAutonomyTickDebugServiceTests.cs`
   - `Desktop/HermesDesktop.Tests/Stardew/StardewPrivateChatWiringTests.cs`

7. 参考项目的关键不是 debug tick，而是：
   - 每个 agent 有自己的 `HERMES_HOME` / `SOUL.md`
   - 有一个常驻 body/server
   - agent 进程长期运行并不断读世界状态 / 做动作
   - 证据：
     - `external/hermescraft-main/civilization.sh:204-212`
     - `external/hermescraft-main/hermescraft.sh:185`
     - `external/hermescraft-main/bot/server.js:3-18`

## 约束

- 计划名称和内容必须用中文。
- 严格对齐参考项目的“常驻 agent”效果，但不机械复制“一 NPC 一进程”。
- 不回退用户已有脏改动。
- 不新增第二套 NPC prompt/agent 装配链。
- 应继续复用这次提交已经建立的 `NpcRuntimeSupervisor` / shared handle 方向，而不是推倒重来。

## 未决问题

1. 常驻 autonomy loop 的 owner 应该直接落在 `NpcRuntimeSupervisor`、`NpcRuntimeHost`，还是新增一个 `StardewNpcAutonomyBackgroundService`？
2. 哪些 NPC 默认启用常驻 autonomy：所有 pack，还是先按 manifest/config 白名单？
3. 与 private-chat 并存时，autonomy 的 pause / resume / 互斥策略应该放在哪一层？
4. MCP discovered tools 变化后，常驻 autonomy runtime 的 rebind 时机怎么定义，避免 mid-turn 热替换？

## 高概率触点

- `Desktop/HermesDesktop/App.xaml.cs`
- `src/runtime/NpcRuntimeHost.cs`
- `src/runtime/NpcRuntimeInstance.cs`
- `src/runtime/NpcRuntimeSupervisor.cs`
- `src/runtime/NpcRuntimeBindings.cs`
- `src/games/stardew/StardewAutonomyTickDebugService.cs`
- `src/games/stardew/StardewNpcRuntimeBindingResolver.cs`
- `src/games/stardew/StardewPrivateChatOrchestrator.cs`
- `Desktop/HermesDesktop.Tests/Runtime/*`
- `Desktop/HermesDesktop.Tests/Stardew/*`
