# Priority Context

# Working Memory

# MANUAL

- 文档命名规则：新建方案、计划、测试规格、上下文快照等文档时，统一使用中文文件名，并带日期。
- 当前本地环境没有 tmux；后续 OMX/深访/计划/执行流程不要依赖 tmux 或 tmux-only 工具需求，能用普通 Codex / PowerShell / 本地文件流程时直接豁免 tmux 前提。
- 用户没有 spark 模型权限；后续不要使用默认 spark 的 `explore` agent / spark 路由。需要子 agent 时改用非 spark 角色，例如 `architect`、`researcher`、`executor`、`code-reviewer` 等。
- Stardew NPC 闲置微动作第一版冷却默认约 10 秒，配置范围围绕 8-15 秒，不要设计成长冷却。


## PRIORITY
自主审查修复计划中文版：.omx/plans/产品需求文档-星露谷非玩家角色自主审查修复.md 与 .omx/plans/测试规范-星露谷非玩家角色自主审查修复.md。覆盖 A1-A5、B1-B6、C1-C2；B5 是预留未生效设置，不是延期。
