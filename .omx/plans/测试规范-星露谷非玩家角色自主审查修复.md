# 测试规范：星露谷非玩家角色自主审查修复

## 目标

证明每个已接受的 autonomy audit 台账项都已经被修复、保留或诚实标为预留未生效，且不依赖上下文记忆。测试必须保护 autonomy parent/local executor 边界、skill 披露归属、timeout 恢复交还、能力面、lane 配置诚实性、prompt fallback 和 `AgentTool` 模板默认值。

## 必测区域

### A4/C1：Autonomy prompt 测试校准

目标文件：
- `Desktop/HermesDesktop.Tests/Runtime/NpcRuntimeContextFactoryTests.cs`

必测用例：
- `Create_AutonomyChannel_OmitsGlobalSkillsMandatoryIndex`
  - 移除对 `Do not call tools` 的过时正向断言。
  - 断言 prompt 包含 raw JSON intent contract 文案。
  - 断言 prompt 不包含 `## Skills (mandatory)`。
  - 断言 prompt 不包含 desktop/browser/web-search guidance。
  - 断言 prompt 不暗示 executor-only tools 可被 parent 调用。

失败信号：
- 测试仍要求出现 `Do not call tools`。
- autonomy channel 收到全局 mandatory skill catalog。
- autonomy prompt 丢失 raw JSON intent contract。

验证命令：

```powershell
dotnet test .\Desktop\HermesDesktop.Tests\HermesDesktop.Tests.csproj -c Debug --filter "FullyQualifiedName~HermesDesktop.Tests.Runtime.NpcRuntimeContextFactoryTests.Create_AutonomyChannel_OmitsGlobalSkillsMandatoryIndex"
```

### C2：审查权威校准

目标文件：
- `agent-autonomy-audit.zh-CN.md` 或旁边的新修订说明

必测/必查：
- 如果仓库已有 docs-test 模式，加入轻量文档断言；否则在最终证据中记录人工验证。
- 旧审查必须指向 `.omx/specs/deep-interview-autonomy-audit-resolution.md` 和中文 PRD，作为当前修复权威。

失败信号：
- 后续读者仍可能把 `agent-autonomy-audit.zh-CN.md` 当唯一事实来源。
- 过时源码判断没有标记。

验证命令：

```powershell
rg -n "deep-interview-autonomy-audit-resolution|产品需求文档-星露谷非玩家角色自主审查修复|superseded|修订|已修订" agent-autonomy-audit.zh-CN.md .omx\plans .omx\specs
```

### A1：Skill 摘要归属

目标文件：
- `src/games/stardew/StardewNpcAutonomyPromptSupplementBuilder.cs`
- `Desktop/HermesDesktop.Tests/Stardew/StardewAutonomyTickDebugServiceTests.cs`

必测用例：
- `RunOneTickAsync_WithRepositoryGamingSkillRoot_UsesSkillOwnedCompactContract`
  - 断言 compact prompt 使用 skill 自有 compact section 或中性结构截取的文本。
- `RunOneTickAsync_WithRepositoryGamingSkillRoot_DoesNotUseHostNeedleSelection`
  - 当 skill asset 中只有硬编码 needle 命中的行被移除或重排时，prompt 生成仍稳定。
- 静态/源码检查：
  - `StardewNpcAutonomyPromptSupplementBuilder` 不得包含用于摘要选择的 `skillId switch`。
  - 不得包含针对 `stardew-core`、`stardew-social`、`stardew-navigation`、`stardew-task-continuity`、`stardew-world` 的硬编码语义 needles 表。

失败信号：
- `BuildCompactSkillContract` 仍使用 `switch skillId`。
- 测试之所以通过，是因为宿主仍在搜索选定语义关键词。

验证命令：

```powershell
dotnet test .\Desktop\HermesDesktop.Tests\HermesDesktop.Tests.csproj -c Debug --filter "FullyQualifiedName~HermesDesktop.Tests.Stardew.StardewAutonomyTickDebugServiceTests"
rg -n "skillId switch|SelectSummaryLines\\(|stardew-core|stardew-social|stardew-navigation|stardew-task-continuity|stardew-world" src\games\stardew\StardewNpcAutonomyPromptSupplementBuilder.cs
```

实现后，`rg` 不应再显示 `skillId switch` 或每个 skill 的语义 needle 表。

### A2：禁止宿主生成 `skill_view(...)`

目标文件：
- `src/games/stardew/StardewNpcAutonomyPromptSupplementBuilder.cs`
- `Desktop/HermesDesktop.Tests/Stardew/StardewAutonomyTickDebugServiceTests.cs`

必测用例：
- `RunOneTickAsync_WithRepositoryGamingSkillRoot_DoesNotAutoInjectSkillViewCalls`
  - 构造一个只引用文件路径但不包含 `skill_view(...)` 的测试 skill。
  - 断言 builder 不合成 `skill_view(name=..., file_path=...)`。
- `RunOneTickAsync_WithSkillOwnedReferenceGuidance_PreservesSkillViewText`
  - 构造一个显式包含 `skill_view(...)` 的 skill asset。
  - 断言 skill 自己写的原文被保留。

失败信号：
- asset 未包含对应文本时，builder 仍添加 `skill_view(name="stardew-world"...` 或 `skill_view(name="stardew-navigation"...`。
- 测试仍要求宿主 fallback injection。

验证命令：

```powershell
dotnet test .\Desktop\HermesDesktop.Tests\HermesDesktop.Tests.csproj -c Debug --filter "FullyQualifiedName~HermesDesktop.Tests.Stardew.StardewAutonomyTickDebugServiceTests"
```

### A3：Timeout 事实交还

目标文件：
- `Desktop/HermesDesktop.Tests/Stardew/StardewNpcAutonomyBackgroundServiceTests.cs`

必测用例：
- `RunOneIterationAsync_WhenActionSlotTimesOut_RecordsTimeoutFactForNextTick`
  - 设置已过期 action slot。
  - 断言结构化 `action_slot_timeout` status/fact 被写入 runtime state 或 activity log。
  - 断言下一轮 autonomy wake 能看到该 status。
- `RunOneIterationAsync_WhenActionSlotTimesOut_CleansResourcesWithoutChoosingRestartStrategy`
  - 断言有 command id 时，过期 command cleanup 或 `CancelAsync` 只作为执行层清理。
  - 断言 world coordination claim 被释放。
  - 断言 service 不用固定 restart cooldown 暂停作为 agent 恢复策略。
- `RunOneIterationAsync_WhenActionSlotTimesOutWithoutCommandId_ClearsSlotAndRecordsFact`
  - 断言没有 synthetic fixed `RetryAfterUtc` 被用作恢复策略。

失败信号：
- timeout 路径调用带 `ActionSlotTimeout` 和固定 restart cooldown 的 `PauseTrackerAsync`。
- agent-visible timeout fact 缺失。
- resource claim 没有释放。

验证命令：

```powershell
dotnet test .\Desktop\HermesDesktop.Tests\HermesDesktop.Tests.csproj -c Debug --filter "FullyQualifiedName~HermesDesktop.Tests.Stardew.StardewNpcAutonomyBackgroundServiceTests"
```

### A4/A5/B4：工具面和能力边界

目标文件：
- `Desktop/HermesDesktop.Tests/Stardew/StardewNpcToolFactoryTests.cs`
- `Desktop/HermesDesktop.Tests/Stardew/StardewNpcAutonomyBackgroundServiceTests.cs`
- `Desktop/HermesDesktop.Tests/Runtime/NpcAgentFactoryTests.cs`
- `Desktop/HermesDesktop.Tests/Stardew/StardewPrivateChatWiringTests.cs`

必测用例：
- `StardewNpcToolSurfacePolicy_DefaultParentSurfaceMatchesCurrentTools`
  - 断言默认 parent tool names 匹配当前安全列表。
- `StardewNpcToolSurfacePolicy_DefaultLocalExecutorSurfaceMatchesCurrentTools`
  - 断言 executor tool names 包含机械执行工具，并排除 speak/private chat。
- `StardewNpcToolSurfacePolicy_OverrideControlsExecutorSurface`
  - 断言测试配置可以 override 某个 tool surface。
- `RunOneIterationAsync_AutonomyParentHasNoDirectAgentToolSurface`
  - 保留现有负向覆盖：parent 没有直接 `agent`、`todo`、`memory` 或 executor-only tool surface。
- `DesktopAndNpcRuntimesUseSharedCapabilityAssembly`
  - 保留 desktop/private chat built-ins 仍通过 `AgentCapabilityAssembler` 接线。

失败信号：
- 工具列表仍只嵌在 factory method 的静态数组里。
- autonomy parent 可见 `stardew_navigate_to_tile`。
- normal desktop/private agent built-ins 被误删。

验证命令：

```powershell
dotnet test .\Desktop\HermesDesktop.Tests\HermesDesktop.Tests.csproj -c Debug --filter "FullyQualifiedName~HermesDesktop.Tests.Stardew.StardewNpcToolFactoryTests"
dotnet test .\Desktop\HermesDesktop.Tests\HermesDesktop.Tests.csproj -c Debug --filter "FullyQualifiedName~RunOneIterationAsync_AutonomyParentHasNoDirectAgentToolSurface|FullyQualifiedName~StardewPrivateChatWiringTests|FullyQualifiedName~NpcAgentFactoryTests"
```

### B3/B5：Lane 路由和 depth 设置诚实化

目标文件：
- `Desktop/HermesDesktop.Tests/LLM/ChatRouteResolverTests.cs`
- `src/LLM/ChatLaneClientProvider.cs`
- `scripts/sync-stardew-npc-config.ps1`

必测用例：
- `Resolve_DelegationLane_UsesLaneSpecificProviderModelAndBaseUrl`
  - 现有 route configurability 保持不变。
- `ChatLaneClientProvider_LogsMaxSpawnDepthAsReservedFlatOnlySetting`
  - 配置了 `delegation.max_spawn_depth` 时，日志必须说明它是预留未生效，或被 flat-only v1 忽略。
  - 日志不得写 `effectiveMaxSpawnDepth=1`，避免看起来像 active enforcement。
- 脚本检查：
  - `sync-stardew-npc-config.ps1` 必须把 `max_spawn_depth` 标成预留未生效，或从 active config 生成结果里移除。

失败信号：
- 日志暗示 configured `max_spawn_depth` 已经生效。
- 同步脚本写入 `max_spawn_depth: 1` 且没有预留/未生效标记。
- lane provider/model/base_url 配置回归。

验证命令：

```powershell
dotnet test .\Desktop\HermesDesktop.Tests\HermesDesktop.Tests.csproj -c Debug --filter "FullyQualifiedName~HermesDesktop.Tests.LLM.ChatRouteResolverTests"
rg -n "max_spawn_depth|预留|未生效|flat-only|effectiveMaxSpawnDepth" src\LLM\ChatLaneClientProvider.cs scripts\sync-stardew-npc-config.ps1
```

### B1/B2：Prompt asset、fallback 与 runtime facts 边界

目标文件：
- `src/Core/SystemPrompts.cs`
- 选定的 prompt asset 路径，例如 `skills/system/stardew-npc-runtime/SYSTEM.md`
- `Desktop/HermesDesktop.Tests/Runtime/NpcRuntimeContextFactoryTests.cs`
- 需要时新增 prompt tests 文件

必测用例：
- `SystemPrompts_LoadsStardewNpcRuntimeFromAssetWhenPresent`
  - 断言 asset 存在时使用 asset 文本。
- `SystemPrompts_MissingAssetFallsBackToCurrentStardewRuntimePrompt`
  - 断言 asset 缺失时确定性 fallback 到当前常量。
- `SystemPrompts_Build_IncludesRuntimeFactsGuidanceForDefaultDesktopPrompt`
  - 断言请求时仍包含 `RuntimeFactsGuidance`。
- `NpcRuntimeContextFactory_AutonomyChannel_OmitsRuntimeFactsGuidance`
  - 断言 autonomy channel 仍排除 runtime facts guidance。

失败信号：
- 缺失 asset 导致普通启动空 prompt 或异常。
- `RuntimeFactsGuidance` 被删除。
- autonomy channel 意外收到 runtime facts guidance。

验证命令：

```powershell
dotnet test .\Desktop\HermesDesktop.Tests\HermesDesktop.Tests.csproj -c Debug --filter "FullyQualifiedName~SystemPrompts|FullyQualifiedName~NpcRuntimeContextFactoryTests"
```

### B6：AgentTool 模板配置化

目标文件：
- `src/Tools/AgentTool.cs`
- `Desktop/HermesDesktop.Tests/Services/AgentToolSchemaTests.cs` 或新的 `AgentToolConfigTests`

必测用例：
- `AgentTool_UsesDefaultAgentDefinitionsWhenNoConfigProvided`
  - 断言 researcher/coder/analyst/planner/reviewer/general 的 prompt 和 allowed tools 匹配当前默认。
- `AgentTool_UsesConfiguredAgentDefinition`
  - 断言 configured agent type 可提供自定义 prompt 和 allowed tool allowlist。
- `AgentTool_InvalidConfiguredToolNamesAreIgnoredOrRejectedDeterministically`
  - 断言非法 tool name 不会暴露意外工具。
- `AgentTool_UnknownTypeFallsBackToGeneralDefinition`
  - 断言 fallback 保持当前 general definition，且不扩大工具面。

失败信号：
- 默认 agent template 行为未被断言就改变。
- invalid config 暴露 allowlist 之外的工具。
- unknown type fallback 变成无边界。

验证命令：

```powershell
dotnet test .\Desktop\HermesDesktop.Tests\HermesDesktop.Tests.csproj -c Debug --filter "FullyQualifiedName~AgentTool"
```

## 覆盖检查表

- A1：测试和源码检查证明没有宿主语义 needle 表。
- A2：测试证明不会生成 `skill_view(...)` fallback。
- A3：测试证明 timeout fact 可见，且恢复策略不由宿主固定。
- A4：测试证明 prompt contract 是当前事实，权限不是 prompt-only。
- A5：测试证明工具面来自 policy，默认不变。
- B1：测试证明 prompt asset 和确定性 fallback。
- B2：测试证明 runtime facts guidance 保留且边界正确。
- B3：测试证明 lane 名和 lane backend routing 稳定。
- B4：测试证明 normal agents built-ins 保留，autonomy parent 排除。
- B5：测试证明 `max_spawn_depth` 是预留未生效设置，不误导。
- B6：测试证明 `AgentTool` template config 和 fallback 行为。
- C1：过时失败断言已经移除。
- C2：旧审查已被标记为已修订或已被新计划取代。

## 完整验证

每个切片实现时先跑聚焦测试，完成时再跑：

```powershell
dotnet test .\Desktop\HermesDesktop.Tests\HermesDesktop.Tests.csproj -c Debug
dotnet build .\Desktop\HermesDesktop\HermesDesktop.csproj -c Debug -p:Platform=x64
```

通过标准：
- 上面所有聚焦测试通过。
- desktop 主测试项目通过。
- desktop build 成功。
- 最终报告逐项列出每个台账编号及其验证证据。
